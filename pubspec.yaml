name: range_bar
description: A customizable Flutter widget for displaying multiple ranges on a single horizontal bar. Perfect for progress indicators, timelines, and data visualization.
version: 0.1.0
homepage: https://github.com/Koichi5/range_bar
repository: https://github.com/Koichi5/range_bar
issue_tracker: https://github.com/Koichi5/range_bar/issues
documentation: https://pub.dev/documentation/range_bar/latest/

topics:
  - ui
  - progress
  - chart
  - range
  - bar

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
